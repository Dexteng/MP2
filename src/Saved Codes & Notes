HERO PAGE SAVED PAGE

No CSS files!
import '../css/Hero1.css'
import '../css/Hero2.css'
import '../css/BestSeller.css'
import '../css/HowItWorks.css'
import '../css/Testimony.css'



Search bar filter Code

<div className="search-container">
        <div className="input-container">
          <input id="searchInput" type="text" placeholder="Search Keyboard..." onChange={(e) => {
            setSearchTerm(e.target.value);
          }} />
        </div>

/*
        const [Search, setSearch] = useState("");
        <div className='items-container'>
        {PRODUCTS
        .filter((item) => {
        if (Search == ""){
        return item;
        } else if (item.name.toLowerCase().includes(Search.toLocaleLowerCase())||
        item.category.toLowerCase().includes(Search.toLocaleLowerCase())){
            return item; 
        }
    })
        .map((product) => (
        <Items data={product} />
    ))}
        </div>


    // Need ma restart ang cart and wishlist para mapa gana ang filtered products


    <div className='cartTab'>
      <h1 className='title'>Shopping Cart</h1>
      <div className="listCart">
        <div className='cart-button'>
          <button className='close-btn'>Close</button>
          <button className='checkout-btn'>CheckOut</button>
        </div>
      </div>
  </div>




CHECK OUT CONTENT

{PRODUCTS.map((product) => {
  if (cartItems[product.id] !== 0 ){
    return <Checkout data={product} />;
  }
})}

import 'bootstrap/dist/css/bootstrap.min.css'

<div className='cart-item-container'>
  {PRODUCTS.map((product) => {
    if (cartItems[product.id] !== 0 ){
      return <CartItem data={product} />;
    }
  })}
</div>